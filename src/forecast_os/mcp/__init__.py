"""MCP server exposing forecast-os to any MCP client.

Run ``forecast-os-mcp`` (requires ``pip install "forecast-os[mcp]"``) and
point your MCP client at it; see :mod:`forecast_os.mcp.server` for the tools.
"""
